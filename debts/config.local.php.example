<?php
/**
 * config.local.php — إعدادات محلية خاصة بالاستضافة
 * ─────────────────────────────────────────────────
 * 1. انسخ هذا الملف باسم: config.local.php
 * 2. عدّل البيانات أدناه ببيانات قاعدة بياناتك
 * 3. config.local.php لن يُلغى أبداً عند التحديثات
 * 4. لا تضمّه في Git (مُضاف لـ .gitignore تلقائياً)
 */

$DB_HOST    = 'localhost';
$DB_NAME    = 'shop_debts';       // اسم قاعدة البيانات
$DB_USER    = 'shop_debts_user';  // اسم مستخدم قاعدة البيانات
$DB_PASS    = 'YOUR_PASSWORD';    // ← عدّل هذا
$DB_PORT    = 3306;
$DB_CHARSET = 'utf8mb4';

define('SITE_URL',  'https://your-domain.com'); // ← عدّل هذا (بدون / في النهاية)
define('SITE_LANG', 'ar');
