<?php
/**
 * install/web_tools_seed.php — one-time web_tools seed data (batch 1).
 * Each row: ['slug'=>, 'title'=>, 'desc'=>, 'icon'=>svg path markup, 'color'=>, 'bg'=>]
 * Empty by default — add tools here if this batch needs to seed real rows;
 * config.php's ensure_schema() marks 'web_tools_seeded' done either way so
 * this only ever runs once per database.
 */
return [];
